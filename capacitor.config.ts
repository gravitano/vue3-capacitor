import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'id.warsono.vuecapacitor',
  appName: 'vue-capacitor',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
